services:
  botmux:
    image: ghcr.io/skrashevich/botmux:main
    container_name: botmux
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - botmux-data:/data
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-}

volumes:
  botmux-data:
